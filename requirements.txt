# Pinned deliberately: `func azure functionapp publish` resolves these
# fresh on Azure's build server, so an unpinned range means an unrelated
# redeploy months from now can pull a new major (icalendar 6.0, for one,
# was a breaking release) and 500 the calendar feed with no code change to
# bisect. Bump these intentionally, not by accident.
azure-functions==1.25.0
azure-data-tables==12.7.0
requests==2.34.2
icalendar==7.3.0
